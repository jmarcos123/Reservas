import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RedefinirPage } from './redefinir';

@NgModule({
  declarations: [
    RedefinirPage,
  ],
  imports: [
    IonicPageModule.forChild(RedefinirPage),
  ],
})
export class RedefinirPageModule {}
